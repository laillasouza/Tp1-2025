<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Calculadora de Regra de 3</title>
</head>
<body>

    <h1>Calculadora de Regra de 3</h1>

        <input style="font-size:12px;" type="number" id="a"> --------- 
        <input style="font-size:12px;" type="number" id="b"> <br>
        <input style="font-size:12px;" type="number" id="c"> --------- x
<br>
    <input type = "button" value="Calcular X" onclick="calcular()">
    <p id="resultado"></p>

    <script>
        function calcular() {
            let A = a.value;
            let B = b.value;
            let C = c.value;

            let x = (B * C) / A;

           resultado.innerHTML =  "X é igual a " + x;
        }
    </script>

</body>
</html>
